import React, { Component } from "react";

export default class ArtcileSearch extends Component {
  state = {
    //isOpen: false,
  };

  handleToggle = () => {
    //this.setState({ isOpen: !this.state.isOpen });
  };

  render() {
    return (
      <></>
    );
  }
}
